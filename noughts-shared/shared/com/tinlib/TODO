TestHelper
  - Get instances from injector instead of making them directly.

EntityGenerator
  - Integrate it with the build process
  - Add support for extensions
  - Add support for packages
  - Generally clean up the code
  - tests

Defer
  - Tests

GameList
  - Fix the tests
  - Rewrite the tests to rely less on storing junk in class fields

General
  - Run tests from command line
  - Run coverage from command line
  - Build javadoc from command line
  - Run findbugs & check style from command line